---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "azureacme_validation_bindings Data Source - azureacme"
subcategory: ""
description: |-
  Every DNS validation binding visible to the caller. Without this, a team cannot enumerate the bindings available to them from Terraform at all.
---

# azureacme_validation_bindings (Data Source)

Every DNS validation binding visible to the caller. Without this, a team cannot enumerate the bindings available to them from Terraform at all.

## Example Usage

```terraform
# Every binding visible to the caller.
#
# The plural form exists because without it a team cannot enumerate the bindings
# available to them from Terraform at all — only from a raw API call they have no
# tooling for.

data "azureacme_validation_bindings" "all" {}

output "healthy_bindings" {
  value = [
    for b in data.azureacme_validation_bindings.all.items : b.id
    if b.healthy
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `items` (Attributes List) (see [below for nested schema](#nestedatt--items))

<a id="nestedatt--items"></a>
### Nested Schema for `items`

Read-Only:

- `delegation_targets` (List of String)
- `healthy` (Boolean)
- `id` (String)
- `last_checked_at` (String)
- `mode` (String)
- `permitted_name_patterns` (List of String)
- `required_cname` (String) When unhealthy, the EXACT record to create.
- `wildcards_allowed` (Boolean) Wildcard is its own axis and is never inferred from a name-scope grant: DNS-01 for `*.api.example.com` and for `api.example.com` uses the SAME challenge name.
- `zone_id` (String)
